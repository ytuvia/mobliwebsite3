/*'use strict';

describe('Service: mobliConfig', function () {

  // load the service's module
  beforeEach(module('website3xApp'));

  // instantiate service
  var mobliConfig;
  beforeEach(inject(function (_mobliConfig_) {
    mobliConfig = _mobliConfig_;
  }));

  it('should have getConfig function', function(){
     expect(mobliConfig.getConfig).not.to.equal(null);
  });

});
*/